public sealed class PressureConfig
{
    /// <summary>
    /// Represents an empty runtime configuration.
    /// </summary>
    public static PressureConfig Empty { get; } =
        new PressureConfig();

    private PressureConfig()
    {
    }
}