public sealed class BossConfig
{
    /// <summary>
    /// Represents an empty runtime configuration.
    /// </summary>
    public static BossConfig Empty { get; } =
        new BossConfig();

    private BossConfig()
    {
    }
}