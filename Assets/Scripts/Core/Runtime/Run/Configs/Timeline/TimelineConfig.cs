public sealed class TimelineConfig
{
    /// <summary>
    /// Represents an empty runtime configuration.
    /// </summary>
    public static TimelineConfig Empty { get; } =
        new TimelineConfig();

    private TimelineConfig()
    {
    }
}