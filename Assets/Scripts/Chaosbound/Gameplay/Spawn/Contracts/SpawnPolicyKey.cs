namespace Chaosbound.Gameplay.Spawn.Contracts
{
    /// <summary>
    /// Identifies the Spawn policy configuration
    /// that should be used for a SpawnRequest.
    /// </summary>
    public enum SpawnPolicyKey
    {
        Default = 0,
        CompletionOrigin = 1
    }
}